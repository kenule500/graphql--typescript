// import { sign } from "jsonwebtoken";
// import { v4 } from "uuid";

// export const createConfirmationURL = async (userId: number) => {
//   const id = v4();

//   await sign( userId, id, { expiresIn: 60 * 60 * 24 });
//   return id;
// };
